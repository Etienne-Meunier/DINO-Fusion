\documentclass[11pt,a4paper]{article}
\usepackage[margin=2cm]{geometry}
\usepackage{graphicx}
\usepackage{booktabs}
\usepackage{amsmath}
\usepackage{amssymb}
\usepackage{siunitx}
\usepackage[hidelinks]{hyperref}
\usepackage{caption}
\usepackage{subcaption}
\usepackage{float}
\usepackage{enumitem}
\captionsetup{font=small}
\setlist[itemize]{nosep, leftmargin=1.2em}
\setlength{\parskip}{3pt}

\title{Conditional diffusion emulator of Veros ACC equilibrium states:\\first end-to-end results}
\author{}
\date{21 September 2026}

\begin{document}
\maketitle

\section*{Summary}
\begin{itemize}
  \item Goal: generate the post-spin-up $(T, S)$ state of the Veros ACC channel directly from the EKE-closure
        coefficients $(c_k, c_\epsilon)$, skipping spin-up.
  \item Pipeline: DINO-Fusion diffusion code adapted to a parameter ensemble; runs end to end on Jean Zay in
        under one A100 hour.
  \item Result: with per-level normalisation the model's hold-out error is $\approx$ the mean-state
        baseline (\SI{0.15}{K}); it beats the nearest training run only when the hold-out gap is large.
        A Wasserstein metric on the stratification profiles gives the same ranking and shows that training
        and held-out conditions score alike.
  \item Physical miss: the model generates temperature inversions at the rate of the weakly eddying runs
        in every regime (Table~\ref{tab:splits}).
\end{itemize}

\section{Data}
\begin{itemize}
  \item 100 Veros ACC runs, $10\times10$ log-spaced grid: $c_k\in[0.0125,\,0.8]$,
        $c_\epsilon\in[0.0875,\,5.6]$; 100 years, 30-day output, interior grid $30\times42\times15$ (x, y, z).
  \item Kept: $T$ and $S$, last 20 years (241 snapshots/run). $S = \SI{35}{psu}$ everywhere in every run.
  \item Nearly deterministic end states: within-window std \SI{0.01}{K} above \SI{1400}{m} vs
        \SI{0.05}{K} between grid neighbours; bottom level still drifts by \SI{0.45}{K} over the window.
  \item One effective parameter: $\;\log c_k - 1.05\,\log c_\epsilon\;$ explains 99\,\% of the variance of
        the domain-mean $T$; the low-$c_k/c_\epsilon$ part of the grid saturates.
\end{itemize}

\section{Method}
\begin{itemize}
  \item Fields $\to$ 30 channels $(T_{1..15}, S_{1..15})$, normalised per level with training statistics,
        land $\to 0$, zero-padded $42\times30 \to 48\times32$:
        \begin{equation}
          x'_{z} = \frac{x_{z} - \mu_{z}}{3\,\max(\sigma_{z}, 0.05)} .
        \end{equation}
        99.4\,\% of the training values lie in the sampler's clip range $[-1,1]$; the exceptions are the
        southern wall between 650 and \SI{1400}{m} and the two bottom levels.
  \item Denoiser: UNet2DModel (64, 64, 128, 128), 6.9\,M parameters. The condition $c$, the standardised
        pair $(\log c_k, \log c_\epsilon)$, enters through an MLP added to the timestep embedding.
  \item DDPM, $\epsilon$-prediction, 1000 steps, squared-cosine schedule, clipping, EMA:
        \begin{equation}
          \mathcal L(\theta) = \mathbb E_{t,\,x_0,\,\epsilon}\,
          \big\| \epsilon - \epsilon_\theta\big(\sqrt{\bar\alpha_t}\,x_0 + \sqrt{1-\bar\alpha_t}\,\epsilon,\; t,\; c\big) \big\|^2 .
        \end{equation}
        20\,000 steps, batch 32, AdamW $10^{-4}$ with cosine decay; \SI{22}{min} on one A100.
  \item Sampling: 1000-step DDPM loop with EMA weights; land and padding re-zeroed after every step;
        32 samples per condition.
\end{itemize}

\section{Evaluation}
\begin{itemize}
  \item Truth: 20-year time-mean state of each held-out run; metrics on water cells.
  \item Baselines: mean training state; nearest training run in standardised log-parameter space;
        average of the axis-neighbour training runs on the grid.
  \item Three hold-out splits, each with its own training set and statistics:
        scattered (10 interior points), band of three rows ($c_k$ = 0.126, 0.2, 0.3175; 30 runs; training rows
        a factor 6.3 apart), top row ($c_k$ = 0.8; 10 runs; extrapolation).
  \item Distributional metric. Each state is reduced to its profile of horizontal means over water cells,
        $\bar T_z$. Per level, the Wasserstein-1 distance between the 32 generated values and all 241 true
        snapshots of the window, through the quantile functions,
        \begin{equation}
          W_1^{(z)} = \int_0^1 \big| F_{g,z}^{-1}(q) - F_{t,z}^{-1}(q) \big|\, dq, \qquad
          W_1 = \sum_z \frac{\Delta z_z}{H}\, W_1^{(z)} ,
        \end{equation}
        with $\Delta z_z$ the level thicknesses from the level midpoints ($H = \SI{2080}{m}$).
        All 241 snapshots are used, not a subsample of 32: the quantile form accepts unequal sizes and a
        subsample would only add noise. A point prediction $x$ (the baselines) gives
        $W_1^{(z)} = \frac{1}{241}\sum_i |x_z - \bar T_{z,i}|$. Unlike the RMSE of the ensemble mean, this rewards a correct
        spread and does not reward collapse to the mean; it ignores horizontal structure, which the RMSE keeps.
\end{itemize}

\begin{table}[H]
\centering\small
\begin{tabular}{lrrr}
\toprule
 & scattered & band of three rows & top row \\
 & 10 runs & 30 runs & 10 runs \\
\midrule
diffusion, ensemble mean of 32: RMSE (K) & 0.150 & 0.129 & 0.193 \\
diffusion, single sample: RMSE (K) & 0.186 & 0.175 & 0.232 \\
nearest training run: RMSE (K) & 0.037 & 0.116 & 0.170 \\
neighbour average: RMSE (K) & 0.020 & 0.116 & 0.170 \\
mean training state: RMSE (K) & 0.155 & 0.197 & 0.470 \\
diffusion: domain-mean bias (K) & $-0.024$ & $-0.023$ & $-0.083$ \\
ensemble spread / true spread (K) & 0.110 / 0.031 & 0.113 / 0.031 & 0.114 / 0.032 \\
$T$ inversions (interfaces), generated / truth & 9.6\,\% / 7.7\,\% & 11.0\,\% / 1.3\,\% & 12.0\,\% / 0.1\,\% \\
max salinity error (psu) & 0.011 & 0.012 & 0.020 \\
\midrule
$W_1$ profile, diffusion (K) & 0.125 & 0.109 & 0.161 \\
$W_1$ profile, neighbour average (K) & 0.036 & 0.095 & 0.142 \\
$W_1$ profile, nearest training run (K) & 0.044 & 0.095 & 0.142 \\
$W_1$ profile, mean training state (K) & 0.136 & 0.160 & 0.420 \\
\bottomrule
\end{tabular}
\caption{Hold-out metrics per split, mean over the held-out runs, water cells only, 32 samples per run.}
\label{tab:splits}
\end{table}

\section{Findings}
\begin{itemize}
  \item \textbf{Conditioning is weak under per-level scaling.} Signal-to-range ratio
        $\rho_z = \sigma_z^{\mathrm{runs}} / (3\sigma_z)$ is 0.7\,\% at the surface, 9\,\% at \SI{650}{m},
        25\,\% at the bottom; the $\epsilon$-loss weights all cells equally, so the shared structure dominates.
        Scattered split: RMSE = mean-state baseline (Figure~\ref{fig:scat}).
  \item \textbf{Model error is split-independent (0.13--\SI{0.19}{K}); baselines degrade with the gap
        (0.02--\SI{0.17}{K}).} The model beats the nearest run and the neighbour average in the two upper band rows and over the
        trending part of the top row ($c_\epsilon$ 0.22--0.88) (Figures~\ref{fig:band}, \ref{fig:top}).
  \item \textbf{Depth structure.} Best method at 650--\SI{800}{m} in the band split
        (\SI{0.08}{K} vs \SI{0.14}{K} for the nearest run); worse than baselines in the top \SI{250}{m} (scale $3\sigma_z \approx \SI{12}{K}$
        amplifies sampler noise) and at the two bottom levels (skewed, drifting, clipped training distribution).
  \item \textbf{Extrapolation is bounded by clipping.} Under the top split's training statistics, 17\,\% of
        the true bottom values of the held-out row exceed $[-1,1]$; generated corner \SI{0.2}{K} too cold,
        bottom RMSE 0.41 vs \SI{0.22}{K} for the row below.
  \item \textbf{Inversions.} Generated 10--12\,\% of interfaces with $\partial_z T < 0$ in every regime;
        truth: 7.7\,\% (scattered) $\to$ 1.3\,\% (band) $\to$ 0.1\,\% (top). The regime dependence is not
        learned.
  \item \textbf{$W_1$ on the mean profiles agrees with the RMSE ranking and adds two facts}
        (right-hand panels of Figures~\ref{fig:scat}--\ref{fig:top}).
        (i) Training grid points score the same as held-out ones (0.123 vs \SI{0.125}{K}, scattered split):
        the model does not fit the training conditions either, the signature of the mean-state collapse.
        (ii) Between 650 and \SI{1200}{m} the samples beat the baselines under $W_1$ in the band and top
        splits, because a point prediction is charged for the spread it cannot represent; the model's total
        is set by the top \SI{250}{m} and the bottom two levels, as for the RMSE.
  \item \textbf{Plumbing.} $S$ within \SI{0.02}{psu} of 35 with no constraint; land exact; ensemble spread
        $3.5\times$ the true within-window spread (Figures~\ref{fig:samples}--\ref{fig:samples_top}).
\end{itemize}

\section{Next steps}
\begin{itemize}
  \item Stratification constraint at sampling time (DINO-Fusion's isotonic projection, on $T$ since $S$ is
        constant): targets the inversion miss directly.
  \item Stronger conditioning: classifier-free guidance; year as a third condition (drift of \SI{0.45}{K}
        at the bottom inside the window).
  \item Evaluate against snapshots as well as the time mean (nearest-snapshot RMSE, spread--skill).
  \item Larger clip range or scale factor where extrapolation matters.
  \item Then: same pipeline on a global Veros configuration (active $S$, seasonal cycle, multi-century spin-up).
\end{itemize}

\paragraph{Reproducibility.} Branch \texttt{miniveros} of the DINO-Fusion repository, package
\texttt{miniveros/}; commits \texttt{f912e66} (scattered split) and \texttt{5991188} (band, top). Extraction
\SI{2.5}{min} (8 cores), training \SI{22}{min} (one A100), generation + evaluation \SI{3.5}{min}.

\begin{figure}[!htb]
\centering
\begin{subfigure}[b]{\textwidth}\centering
\includegraphics[width=\textwidth]{../full_3std/eval/grid_maps.png}
\caption{Over the parameter grid: domain-mean $T$ of the truth and of the generated states, their difference, and $W_1$ of the profile distribution. White dots: training runs; red crosses: held-out runs.}
\end{subfigure}\\[4pt]
\begin{subfigure}[b]{\textwidth}\centering
\includegraphics[width=0.62\textwidth]{../full_3std/eval/profiles.png}
\caption{RMSE (left) and $W_1$ (right) by depth, mean over the held-out runs.}
\end{subfigure}
\caption{Scattered split (10 interior held-out runs), 32 samples per run.}
\label{fig:scat}
\end{figure}

\begin{figure}[!htb]
\centering
\begin{subfigure}[b]{\textwidth}\centering
\includegraphics[width=\textwidth]{../band3_3std/eval/grid_maps.png}
\caption{Over the parameter grid: domain-mean $T$ of the truth and of the generated states, their difference, and $W_1$ of the profile distribution. White dots: training runs; red crosses: held-out runs.}
\end{subfigure}\\[4pt]
\begin{subfigure}[b]{\textwidth}\centering
\includegraphics[width=0.62\textwidth]{../band3_3std/eval/profiles.png}
\caption{RMSE (left) and $W_1$ (right) by depth, mean over the held-out runs.}
\end{subfigure}
\caption{Band of three rows ($c_k$ = 0.126, 0.2, 0.3175 held out), 32 samples per run.}
\label{fig:band}
\end{figure}

\begin{figure}[!htb]
\centering
\begin{subfigure}[b]{\textwidth}\centering
\includegraphics[width=\textwidth]{../top_3std/eval/grid_maps.png}
\caption{Over the parameter grid: domain-mean $T$ of the truth and of the generated states, their difference, and $W_1$ of the profile distribution. White dots: training runs; red crosses: held-out runs.}
\end{subfigure}\\[4pt]
\begin{subfigure}[b]{\textwidth}\centering
\includegraphics[width=0.62\textwidth]{../top_3std/eval/profiles.png}
\caption{RMSE (left) and $W_1$ (right) by depth, mean over the held-out runs.}
\end{subfigure}
\caption{Top row ($c_k$ = 0.8 held out, extrapolation), 32 samples per run.}
\label{fig:top}
\end{figure}

\begin{figure}[p]
\centering
\includegraphics[width=0.72\textwidth]{../full_3std/samples_levels.png}
\caption{Scattered split, $c_k=0.2$, $c_\epsilon=2.222$: true 20-year mean (left) and three random samples at
the surface, \SI{650}{m} and the bottom; $T$ (top three rows), $S$ (bottom three rows). Grey: land.}
\label{fig:samples}
\end{figure}

\begin{figure}[p]
\centering
\includegraphics[width=0.72\textwidth]{../band3_3std/samples_levels.png}
\caption{Band-of-three split, same condition ($c_k=0.2$, $c_\epsilon=2.222$, now in the middle held-out row).}
\label{fig:samples_band}
\end{figure}

\begin{figure}[p]
\centering
\includegraphics[width=0.72\textwidth]{../top_3std/samples_levels.png}
\caption{Top-row split, $c_k=0.8$, $c_\epsilon=0.5556$ (extrapolation one row above the training grid).}
\label{fig:samples_top}
\end{figure}

\end{document}
